# FPV Frequency Analyzer - Project Summary

## 🔹 Описание проекта
**FPV Frequency Analyzer** – это инструмент для анализа совместимости каналов FPV-видеопередачи. Он помогает пилотам **выбирать частоты**, минимизируя **помехи и перекрытие каналов**.

### 🔹 Основные функции
- Загрузка данных о FPV-частотах (аналоговые и цифровые системы).
- Фильтрация частот по **региону (FCC, CE)**, **типу модуляции (Analog/Digital)** и **ширине канала**.
- Анализ **перекрытия частот** между каналами (🟢 безопасные, 🔴 критически перекрытые).
- Интерфейс с **визуальным выводом данных и анимацией**.

## 🔹 Структура проекта
📂 **root/** *(Корень проекта)*  
├── **backend/** *(Логика сервера и анализа данных)*  
│   ├── `app.py` – Flask-сервер, API для данных.  
│   ├── `config.py` – настройки, пути к JSON, параметры.  
│   ├── **fpv_logic/** *(Логика анализа частот)*  
│   │   ├── `__init__.py` – делает папку модулем.  
│   │   ├── `data_loader.py` – загружает JSON при запуске, сохраняет в памяти сервера.  
│   │   ├── `frequency_filter.py` – обрабатывает фильтры (`region`, `modulation`, `bandwidth`).  
│   │   ├── `interference.py` – анализирует пересечения частот, зоны перекрытия.  
│   ├── **data/** – JSON-файлы с частотами.  
│
├── **static/** *(Фронтенд файлы)*  
│   ├── **js/** *(Каталог с фронтенд-скриптами)*  
│   │   ├── `script.js` – главный файл, подключен к `index.html`, загружает JSON один раз.  
│   │   ├── `matrix.js` – анимация матрицы, подключен напрямую в `index.html`.  
│   │   ├── `ui.js` – функции для работы с интерфейсом, позже пригодится.  
│   │   ├── `dataLoader.js` – загружает JSON один раз, сохраняет в `sessionStorage` или `localStorage`.  
│   │   ├── `fpvAnalyzer.js` – обрабатывает данные: фильтры, расчет интерференции и сортировка каналов.  
│   ├── **css/** *(Стили проекта)*  
│
├── **templates/** – `index.html`, основной UI.  
├── **TODO.md** – Список задач.  
├── **project_summary.md** *(Этот файл)*  

## 🔹 Основные API-эндпоинты (`app.py`)
| Метод | URL | Описание |
|--------|----------------|--------------------------------|
| `GET` | `/` | Главная страница (HTML) |
| `GET` | `/api/data` | Получение всех частот (JSON) |
| `GET` | `/api/filters/region?region=FCC` | Фильтрация по региону |
| `GET` | `/api/filters/modulation?modulation=Analog` | Фильтр по модуляции |
| `GET` | `/api/filters/bandwidth?bandwidth=20` | Фильтр по ширине канала |
| `GET` | `/api/frequency?band=Raceband&channel=5` | Получение частоты |
| `GET` | `/api/interference?band=F&channel=4` | Анализ интерференции |

## 🔹 Оптимизация загрузки данных (Backend & Frontend)
Чтобы ускорить работу приложения, JSON-файл с частотами загружается **только один раз** при запуске, а затем **используется локально на клиенте**.

### 🔹 Backend
- `data_loader.py` **загружает JSON при старте Flask** и сохраняет его в памяти.
- `/api/data` **отдает весь JSON мгновенно**, без лишних запросов к файлу.

### 🔹 Frontend
- `dataLoader.js` **грузит JSON один раз и сохраняет его в `sessionStorage`**.
- Все фильтры (`fpvAnalyzer.js`) работают **локально**, не перегружая сервер.
- `script.js` загружает интерфейс **моментально**, а данные подтягиваются асинхронно.

### 🔹 Как это работает?
1. Браузер загружает `index.html` и `script.js` моментально.
2. `dataLoader.js` отправляет один запрос к `/api/data`, сохраняет ответ.
3. Фильтры и анализ каналов работают **с локальной копией данных**.
